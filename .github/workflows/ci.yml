name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - run: python -m pip install -e '.[dev,voice,robot]'
      - run: python -m ruff check src tests
      - run: python -m ruff format --check src tests
      - run: python -m mypy --strict src
      - run: python -m pytest --cov=cozmo_tr --cov-branch --cov-fail-under=85
      - run: python -m pip_audit
